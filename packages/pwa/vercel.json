{
  "buildCommand": "cd packages/pwa && npm install && npm run build",
  "outputDirectory": "packages/pwa/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://api.sella.co/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
