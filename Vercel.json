{
  "buildCommand": "cd packages/web && npm install && npx vite build",
  "outputDirectory": "packages/web/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://api.sella.co/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
